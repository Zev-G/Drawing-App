package app.misc;

import javafx.scene.paint.Color;

public final class Settings {

    // PERFORMANCE
    public static boolean USE_GLOSS_EFFECT = true;

    // GENERAL
    public static Color[] DEFAULT_COLOR_PRESETS = { Color.BLACK, Color.WHITE, Color.RED, Color.BLUE };

}
